using UnityEngine;

public class CameraFollow : MonoBehaviour
{
    /*
    public Transform player;        // Reference to the player's transform
    public Vector2 offset;         // The offset distance between the camera and the player
    public float smoothSpeed = 0.125f; // Smoothness factor (higher value means less smooth)

    void LateUpdate()
    {
        // Desired position of the camera (player's position + offset)
        Vector2 desiredPosition = player.position + offset;

        // Smoothly move the camera towards the desired position
        Vector2 smoothedPosition = Vector3.Lerp(transform.position, desiredPosition, smoothSpeed);

        // Update the camera's position
        transform.position = smoothedPosition;
    }
    */
}